#include "bio.h"

#include <string>
using std::string;

#include <sstream>
using std::istringstream;


    /*string tested_string = "";
    istringstream iss(input_dna);
    char ch;
    while (iss.get(ch)){
        tested_string +=ch;
    }*/
    
/*bool IsValidDNASequence(string input_dna){
    for (auto& x:input_dna){
        if ((x == 'A') || (x == 'T') || (x == 'C') || (x == 'G')) { 
            continue;
        } else {
            return false;
        }
    }
    return true;
}*/

string GetReverseComplementSequence(string input_sequence){
    
}